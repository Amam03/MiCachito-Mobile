<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- Opt-out del back predictivo (default-ON con targetSdk 36): sin esto
	     el sistema NO llama a MainActivity.OnBackPressed y el back en raíz
	     finaliza la Activity -> crash MAUI 10 (ShellFragmentContainer.OnDestroy
	     con IServiceProvider disposed, dotnet/maui#32750). Con opt-out, el
	     back en raíz manda la app a segundo plano (comportamiento estándar). -->
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" android:enableOnBackInvokedCallback="false"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.CAMERA" />
	<!-- Impresión Bluetooth de tickets (mockup 5.1): legado + runtime Android 12+ -->
	<uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
	<uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
	<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
	<uses-feature android:name="android.hardware.bluetooth" android:required="false" />
	<uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>